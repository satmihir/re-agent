# Copy to .env and fill in. .env is ignored by git. Plain KEY=value lines;
# make live sources this file with sh, so no spaces around the equals sign.
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...

# Optional: which OpenAI model the live test requests. Defaults to the
# compiled default.
# REAGENT_TEST_MODEL=gpt-5.6-luna
